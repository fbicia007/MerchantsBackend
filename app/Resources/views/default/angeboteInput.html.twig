{% extends 'base.html.twig' %}

{% block content %}
    {{ parent() }}
    <div class="container">
        <h2>Angebote Infomation</h2>

        {{ form_start(myForm) }}
        {{ form_widget(myForm) }}
        <button type="submit" class="btn btn-primary">Save</button>
        <a href="/">
            <button type="button" class="btn btn-danger">Cancel</button>
        </a>
        {{ form_end(myForm) }}




    </div><!-- /.container -->
{% endblock %}

{% block stylesheets %}
{% endblock %}
